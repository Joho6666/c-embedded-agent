# ADR 0002: Unified tool policy

Status: Accepted.

Each tool has one specification containing its schema, handler, side effect, availability and approval requirement. The same specification feeds model exposure and dispatch. This makes permission enforcement auditable and prevents declarations from drifting away from executable behavior.
